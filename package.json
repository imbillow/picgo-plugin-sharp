{
  "name": "picgo-plugin-sharp",
  "version": "1.1.0",
  "description": "Image transformer plugin, Can be used for image compression",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/iovw/picgo-plugin-sharp",
  "scripts": {
    "test": "no test",
    "lint": "eslint src --ext .js,.jsx,.ts,.tsx",
    "build": "tsc -p .",
    "dev": "tsc -w -p .",
    "patch": "yarn version --patch && git push origin master && git push origin --tags",
    "minor": "yarn version --minor && git push origin master && git push origin --tags",
    "major": "yarn version --major && git push origin master && git push origin --tags"
  },
  "keywords": [
    "picgo",
    "picgo-plugin"
  ],
  "author": "iovw",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^8.1.0",
    "@types/node": "^10.17.17",
    "@types/request": "^2.48.4",
    "@types/sharp": "^0.24.0",
    "@typescript-eslint/eslint-plugin": "^2.23.0",
    "@typescript-eslint/parser": "^2.23.0",
    "eslint": "^6.8.0",
    "picgo": "^1.4.0",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "fs-extra": "^8.1.0",
    "image-size": "^0.8.3",
    "sharp": "^0.25.1"
  }
}
